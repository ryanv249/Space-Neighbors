export default class Notebook{
    constructor(scene, entries){
        // for every planet string in entries, provide planet description object 
        // place all description texts into center of screen, place pictures around (they are separate)
    }
}